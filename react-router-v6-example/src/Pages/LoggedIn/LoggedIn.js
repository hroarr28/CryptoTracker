import React from "react";
// on this page we will allow the user to search for a crypto and save it to faviorites
// we will also display the faviorites
// we will also display an 

function LoggedIn() {
    return (
        <div>
        <h1>Logged In</h1>
        </div>
    );
}
    
export default LoggedIn;